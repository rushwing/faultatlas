apiVersion: v1
kind: Service
metadata:
  name: retriever-svc
  namespace: faultatlas
spec:
  selector:
    app: retriever
  ports:
    - port: 8001
      targetPort: 8001
  type: ClusterIP   # Internal only — not exposed externally
