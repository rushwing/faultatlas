name: Requirements

on:
  pull_request:
  push:
    branches: [main]

jobs:
  requirements:
    name: Requirement Format Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate requirement files
        run: bash scripts/ci/check-requirements.sh
